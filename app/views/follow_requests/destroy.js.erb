console.log("Howdy from follow request.")

var followRequest = $(".<%= dom_id(@follow_request.sender)%>_<%= dom_id(@follow_request.recipient) %>");

var updatedFollowRequest = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient)%>");

console.log(followRequest)

followRequest.replaceWith(updatedFollowRequest)
